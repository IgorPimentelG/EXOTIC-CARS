export type Car = {
    id: number;
    brand: string;
    model: string;
    rent_price: number;
    image: string;
    squeri?: string;
};

export type CarDetail = {
    id: number;
    image: string;
    color: string;
};